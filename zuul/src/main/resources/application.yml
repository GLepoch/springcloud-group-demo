spring:
  application:
    name: zuul
  datasource:
    username: root
    password: 123
    type: com.alibaba.druid.pool.DruidDataSource
    url: jdbc:mysql://localhost:3306/security_authority
server:
  port: 4001
  servlet:
    session:
      cookie:
        name: zuulck
eureka:
  instance:
    prefer-ip-address: true
    instance-id: ${spring.cloud.client.ip-address}:${server.port}
  client:
    service-url:
      defaultZone: http://eureka-a:1001/eureka

zuul:
  sensitive-headers: Cookie,Set-Cookie
#  ignored-services: "*"
#  routes:
#    glepoch:
#      path: /glepoch/**
#      service-id: provider
#    consumer: /gl/**
security:
  oauth2:
    client:
      client-secret: 123
      client-id: zuul
      user-authorization-uri: http://127.0.0.1:9001/oauth/authorize
      access-token-uri: http://127.0.0.1:9001/oauth/token
    resource:
      user-info-uri: http://127.0.0.1:9002/user

