server:
  port: 5001

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
#        health:
#          repositories:
#            myservice:
#              label: mylabel
#            myservice-dev:
#              name: myservice
#              profiles: development
        git:
          uri: https://github.com/GLepoch/springcloud-group-demo.git
          searchPaths: configserver/comm-config,configserver/single-config/{application}
          username: GLepoch  #账号密码写真实的快一些我觉得，不使用也能访问有点慢
          password: l537073101
encrypt:
  key-store:
    location: classpath:configserver.jks
    alias: mytestkey
    password: 111111
    secret: 111111
